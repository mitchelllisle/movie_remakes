CREATE TABLE movies (
  tconst varchar,
  title_type varchar,
  primary_title varchar,
  original_title varchar,
  is_adult varchar,
  start_year varchar,
  end_year varchar,
  runtime_mins varchar,
  genres varchar);

CREATE TABLE ratings (
tconst varchar,
average_rating int,
num_votes int);
